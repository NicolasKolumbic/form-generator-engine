export const errorMessages = {
    "minlength": "The name %fieldName% must contain at least %count% characters",
    "maxlength": "The %fieldName% field can contain a maximum of %count% characters",
    "required": "The %fieldName% field is required",
    "pattern": "The %fieldName% field has an incorrect format",
}